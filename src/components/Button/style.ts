import styled from 'styled-components'
import { cores } from '../../styles/styles'

export const ButtonContainer = styled.button`
  background-color: transparent;
  color: ${cores.textoPrimaria};
  border: 2px solid ${cores.textoPrimaria};
  padding: 8px 16px;
  border-radius: 8px;
  font-size: 16px;
  font-weight: bold;
`
